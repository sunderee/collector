name: numberpicker
description: Custom number picker for Collector inspired by numberpicker package
version: 0.0.1

environment:
  sdk: ">=2.17.6 <3.0.0"
  flutter: ">=3.0.5"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  # Linter
  flutter_lints: ^2.0.1