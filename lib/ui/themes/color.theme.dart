import 'package:flutter/material.dart';

class ColorTheme {
  static const Color colorProduct = Color(0xFF20A4F3);
  static const Color colorBackgroundLight = Color(0xFFFAFAFA);
  static const Color colorBackgroundDark = Color(0xFF212121);

  const ColorTheme._();
}
